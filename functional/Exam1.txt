models of computation
procedural
fucntional
logic
object-oriented
security

Procedural




Fucntional

Why is SQL not a procedural style language

why is fucntional style connected to immutabel objects

why would you chose a language that is strictly fucntional style insted of general purpose

covert the code to fucntional style

what are advantages to fucntional style


Logic/Constraint

why are essentially all UI layout languages constraing languages

why is logic style a terrible idea for an emergancy response procedure manueal

what are advantages of logic style programming

how would you modify the following prolog so the query would include 'peanuts'

like(mike, cheese)
like(mike, eggs)
like(mike, peanuts)
like(sue, bike)
like(sue, kites)
like(Thing) :- like(mike, Thing),
like(sue, Thing)

likes(thing)

Why is SQL a logic language


Security

Why is lack of public/private a security concern in a language

JSON and XML are data languages for communication between systems. Why are these nog=t general purpose languages

C++ is high preformance, but allows for memory problems such as buffer overflow. Java and C# always check. 
Why are low level tools like video decoders written in C/C++ and service tools API's for, say amaon orders, are written in Java

You are asked to build a web service for the internet-of-things where each switch and light bult in a corporation is controllable
from the system. WHat languages would you chose and why?

Now you work at Levi's and building their order fullfillment system. Now which one and why

Why does apple not allwo scripting languages in their App store